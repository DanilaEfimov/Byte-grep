# Byte-grep
byte-grep is a command-line utility that searches for byte and bit patterns in files and directories. Supports custom masks for binary signature matching, forensic analysis, and reverse engineering tasks.
